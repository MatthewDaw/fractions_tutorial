# Work Order — r2 (Unlike Denominators · Cross-Multiply framing)

- **Component:** `web/src/LessonUnlikeDen.jsx` (SHARED with r3 — see contention) · **Route:** `#/r2` · **Effort:** L
- **Registry:** `web/src/lessons/r2.js` (port from `docs/wireframe/src/lessons/r2.js`)
- **Uses shared shell:** yes
- **⚠ CONTENTION:** `LessonUnlikeDen.jsx` is the same file as r3. **Do NOT run r2 and r3 in parallel.** Assign both to one agent OR land r2 first, rebase r3.

## Target wireframe screens
`docs/wireframe/screens/room-r2-2-bind.html`, `-3-fade.html`, `-5-ghost.html`, `-6-numbers.html`, `-7-words.html`, `-w-workbench.html`, `-a-applied.html`, `-sw-showwork.html`, `-practice.html`.

## Design gaps to close
- Beat navigation: LU owns its own `_setBeat`/`NEXT_BEAT` ladder + `BEATS` array (`:789-800`) — centralize the TAB STRIP in registry, **but keep beat transition logic in component**.
- Top-level render branches (isPractice/isLW/isLA/isSW/isL7) bypass standard board — drive layout (split/wide) from beat metadata.
- Goal/rail/status/picker copy inline → beat metadata fields.
- Two-stage answer gate (den/num); applied setup gate; showwork gate — parameterize gate TYPES (state stays in LU).
- Cross-multiply `.lu-cross` SVG → extract `<CrossMultiplyDiagram>` gated by `lesson.framing.crossMultiply`.

## Dedup moves
- `lessons/r2.js`: id/num/tag/title/route/tabs + `layout:{railW:360,footH:150}` + beat metadata (statusMessages, goalCopy, railType, pickerConfig).
- Rail content factory (knives/picker/L7/bare) reading `beat.railType`.
- Extract `<CrossMultiplyDiagram>`, optional `<ShowWorkStage>`, strip render loop.

## Mechanics to PRESERVE (CRITICAL)
- LU owns beat state + `NEXT_BEAT` ladder + post-RaiseScaffold hold + deferred-on-Next raise — **must not move to shared hook**. Hook is engine/practice only.
- Knife drag→cut (`grabKnife`/`hitBar`); strip drag→join (collision, shake, auto vs manual); picker `applyCommon`/`setM` (L2 manual join, L3/L4 auto); two-stage gate; applied setup; showwork ink; workbench `onSandboxSolve`; error signatures; handwriting/InkPad path + ink confidence + `logCheck` dev endpoint; voice keys; ruler coords (ORIGIN/SPAN/UNIT/LINE_Y/BAR_H/HOME).

## Playwright test plan
Drive each tab (1-5, W, A, ✎, ★): beat navigation L0→L2→L4→LW→L5→L6→LA→SW→L7→practice; slice via knife; join (mismatch shakes); picker (disable on match, invalid pick warns); two-stage answer (den unlocks num); applied setup (reversed order OK); showwork (Next gated on ink); words (wide, New recipe cycles, Band null); workbench BlockSandbox; practice GenPracticeBoard; cross-multiply arrows render on L5/L6; cook idle/think/cheer; handwriting vs Slate; error signatures (add_denominators etc.); reset/re-deal.

## Risks
Beat ownership (preserve one-question-per-level); five render branches change layout; ungraded gates stay in state; word problems point to `lesson.wordProblems`; handwriting modality + ink confidence; cross-multiply must not bleed into scaleOne; ruler coords centering; generated bank; engine hook untouched; perf on beat switch (memoize).

## Definition of done
Wireframe match · registry tabs + beat metadata · beat logic preserved · Playwright passes, zero errors · build clean · vitest green · worktree + PR. **r3 rebases on this.**

# Work Order — r5 (Lesson 10 · Improper → Mixed)

- **Component:** `web/src/AppR5.jsx` · **Route:** `#/r5` · **CSS:** `r5.css` · **Effort:** M
- **Registry:** `web/src/lessons/r5.js` (port from `docs/wireframe/src/lessons/r5.js`)
- **Uses shared shell:** yes · Disjoint; parallel-safe.

## Target wireframe screens
`docs/wireframe/screens/room-r5-2-bind.html`, `-3-fade.html`, `-4-numbers.html`, `-5-words.html`, `-applied.html`, `-practice.html`, `-sw-showwork.html`, `-workbench.html`.

## Design gaps to close
- STAGES (`:88-98`) hardcoded → registry tabs (badges 1,2,3,W,4,A,✎,5,★).
- QuestionBand hidden on Words+Practice (matches — verify).
- Slate den colors from `denominatorColors.js` (correct per problem).
- Topbar №10 dynamic (no gap, verify registry value).
- `.joinbtn` keyboard affordance exists in code but not in static wireframe — verify it renders unsolved.

## Dedup moves
- STAGES, `probForStage()`, `stageGreeting()`, error/hint strings → `lessons/r5.js` (config object with per-stage problem/greeting/messages).
- Board geometry constants (ORIGIN/SPAN/UNIT/FRAME_*) → registry/CSS vars.
- Extract `<MixedNumberComposer>` (`.r5-write` whole/and/frac) from inline JSX; reuse across bind/fade/numbers/words/applied.
- Extract `<BlockGrid>`, `<Ruler>`; voice keys → registry/voice config.

## Mechanics to PRESERVE
- Drag-to-place blocks (`grabBlock`, ghost portal), group-count feedback, frame lock; Stage-3 choose-wholes buttons (reachability filter); Slate mixed-number write + `checkWritten` (exact-whole trap 14/7=2); Stage-1 manipulate-as-solve (auto finishSolved); `.joinbtn` keyboard; workbench `onSolve`; applied setup gate; words optional scratch (no gate); showwork mandatory ink gate; auto-advance deferred 1500ms; problem swap per stage; ghost-board fade Stage 3.

## Playwright test plan
1. Stage 1: drag 7 blocks into frame → locked, "all grouped ✓", auto-solve.
2. Stage 2 bind: write 1 whole + 2/7 leftover → Check → cheer.
3. Stage 3 fade: dimmed board, choose-wholes; click 1 → ok; click 2 → "Not quite"; then write mixed.
4. Stage 4 numbers exact-whole: 14/7; write 2, leave leftover empty → accept.
5. Stage 5 words: Band hidden; scratch optional; write 2 3/4 → solve.
6. Applied: setup 11/4 → "Check the fraction" → unlock; 2 3/4 → solve; wrong setup 11/7 → shake.
7. Show Work: ink → Next (ungraded). 8. Workbench: build 9/7 → onSolve.
9. Practice: GenPracticeBoard IMPROPER_TO_MIXED. 10. Tabs/back/reset. 11. Ghost portal. 12. Keyboard `.joinbtn`. 13. Den color consistency. 14. Voice. 15. Rosette stars.

## Risks
Window pointer listeners; `.joinbtn` visibility flag; exact-whole REMAIN===0 guard; placedRef/probRef sync (useEffect deps); cook timer conflicts on rapid advance; BlankSlate spurious ink; setupOk permanence; resetStage prob/probRef sync; engine node `IMPROPER_TO_MIXED` exists; ghost portal SSR guard; zoom/scale drag bounds; choose-wholes filter; GenPracticeBoard award; parameterize story for A/B.

## Definition of done
Wireframe match · registry tabs/problems/greetings · Playwright passes, zero errors · build clean · vitest green · worktree + PR.

# Work Order — r3 (Non-Unit / Scale-One framing)

- **Component:** `web/src/LessonUnlikeDen.jsx` (SHARED with r2) · **Route:** `#/r3` · **Effort:** M
- **Registry:** `web/src/lessons/r3.js` (port from `docs/wireframe/src/lessons/r3.js`)
- **Uses shared shell:** yes
- **⚠ CONTENTION:** same file as r2. **Land r2 first, then rebase r3 onto it.** Do NOT run in parallel.

## Target wireframe screens
`docs/wireframe/src/lessons/r3.js` + `docs/wireframe/screens/room-r3*.html` (room-r3-2-bind, -3-fade, -6-numbers, etc.).

## Design gaps to close
- Same shared component as r2; r3 is Scale-One framing → `showCross=false` (no crossing visual on bare beats).
- Lesson number/identity inline → registry (r3 = Lesson 2 per wireframe `r3.js:6`; reconcile numbering in the central renumber pass).
- Tab strip (10 beats) hardcoded in `BEATS` → registry tabs.
- Goal/rail/answer/tutor copy inline → beat metadata.
- Answer-slot hue dynamic (matched D); two-stage gate identical to r2.

## Dedup moves
Same as r2 — most extractions (`<CrossMultiplyDiagram>`, rail factory, beat metadata, status messages, `lesson.voice` lookup) are shared. r3 mainly authors `lessons/r3.js` and verifies the Scale-One branch (no crossing arrows).

## Mechanics to PRESERVE
Knife drag/cut; strip drag/join; denominator picker (L2 manual / L3-L4 auto); slice counter + reset (selfCorr); two-stage answer gate; generated bank (L4); word problems (L7); voice; engine emission (errorSignature/surfaceForm); `showCross=false` for Scale-One.

## Playwright test plan
1. Tab 2 (Bind): isL2, picker 3 choices, den slot open / num locked.
2. Knife: drag n=2 onto strip A (3/8) → applyCut, mA=2, tick++, ×2 badge.
3. Picker: choose 8 → applyCommon, denStr=8, worksheet, picker disables; drag B→A → doJoin, Combined plank, num unlocks.
4. Two-stage: write 8 Check → stage 'num'; write 5 → finishSolved, 3 stars, Next.
5. Reset/ladder L2→L4: faded strips, auto-join on pick.
6. Bare beats (L6): no strips, bareEntry skips matched gate.
7. Workbench/applied/showwork/words/practice as r2.
8. Cross-multiply: r3 shows NO `.lu-cross` (plain slate-eq); confirm absent.
9. Error: wrong numerator → signature, shake, say('notQuite').

## Risks
Shared-component coupling with r2 (rebase carefully); knife scale `k` stale (recompute per move); `matchedRef` batching; selfCorr semantics; two-stage reset; bank/wpIndex bounds (modulo guard); InkPad null confidence; cross-multiply guard (must stay off); ruler coords; z-index on knife-layer; workbench mixed-number ruler extent.

## Definition of done
Wireframe match (Scale-One, no crossing) · `lessons/r3.js` authored · rebased cleanly on r2 · Playwright passes, zero errors · build clean · vitest green · worktree + PR.

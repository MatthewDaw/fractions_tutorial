# Work Order — r1 (Lesson 4 · Adding Fractions, Same Denominators)

- **Component:** `web/src/AppR1.jsx` · **Route:** `#/r1` · **Effort:** L
- **Registry:** `web/src/lessons/r1.js` — **AUTHOR NEW** (wireframe has no `r1.js`). `{ id:"r1", num:"№4", tag:"Adding Fractions", title:<title>, route:"#/r1", tabs:[Manipulate, Bind, Fade, Workbench, Numbers, Applied, sw Show Work, Words, ★Practice] }` from STAGES (`AppR1.jsx:70-85`).
- **Uses shared shell:** yes · Independent of r2/r3.

## Target wireframe screens
`docs/wireframe/screens/room-r1*.html` (room-r1, -2-bind, -3-fade, -4-workbench, -5-numbers, -6-applied, -sw-showwork, -7-words, -practice).

## Design gaps to close
- Stage 1: two draggable stacks (HOME.A/B) merge → static merged in wireframe (mechanics OK, visual diff).
- Stage 4 workbench renders BlockSandbox+hud as fragment outside LessonBoard.
- Stage 6 ExpressionSlate (setupA/setupB), "Check the sum" label.
- Word stages 6/sw/7 wrapped in `FitStage axis='y'`.
- Stage 7 omits QuestionBand (intentional); practice GenPracticeBoard.

## Dedup moves
- Create `lessons/r1.js`; move STAGES → tabs; `toStageVal`/`SCAFFOLD_KEY`/advance order become registry-data-driven.
- `STAGE_INTRO` (`:878-890`, 8 branches) → registry `intros`.
- Identity `tag/title/no` → registry (currently hardcoded `:855-857`).
- Chrome already deduped via LessonShell — keep.

## Mechanics to PRESERVE
- Stage 1 drag-to-merge (`grabBar`/`dropBar`, near threshold), unmerge, numeric Slate, denominator-lock.
- `gradeSlate` (locked den on stage 3); Stage 4 `onSandboxSolve` (same-size only); Stage 6 order-agnostic setup gate; sw ink gate; auto-advance via `advanceByOrder`.

## Playwright test plan
1. Smoke: topbar "№4 Adding Fractions", goal text, QuestionBand "2/7 + 3/7 = ?", 9 tabs, back/rewatch/reset.
2. Stage 1: drag A & B together → merge, "Same-size pieces…"; enter 7 → "bottom locked"; enter 5 → award, advance; unmerge.
3. Stage 2: empty → Check inert; 5/7 → award.
4. Stage 3: ghosted stack, locked den; 5 → award.
5. Stage 4: drag 1/7 chips to 5/7 → onSolve, "Build it ▸"; wrong size never solves.
6. Stage 5: bare eq; 5/7 → advance; 5/9 → "keep bottom 7".
7. Stage 6: wide variant; setup 2/7+3/7 (and reversed) passes; answer Slate gated; 5/7 → advance.
8. sw: draw → Next enables.
9. Stage 7: QuestionBand hidden; 5/7 → final award, onEnd.
10. Practice: fresh ADD_SAME_DEN. 11. Deep link `?stage=5`; rapid tab clicks. 12. A11y/ARIA. 13. Visual: tick spacing 56px, hue per den.

## Risks
useRef position sync (test with Playwright pointer API); BlockSandbox same-size contract; setup parse (leading zeros); `say()` may hang (mock voice); resetStage completeness; engine decision (stumpingRecipe path); QuestionBand must be null on 7/practice; LessonBoard overlap on mobile.

## Definition of done
Wireframe match · `lessons/r1.js` authored + consumed · Playwright passes, zero errors · build clean · vitest green · worktree + PR.

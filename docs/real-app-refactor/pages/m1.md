# Work Order — m1 (Lesson 1 · Equal Groups)

- **Component:** `web/src/AppM1.jsx` · **Route:** `#/m1` · **CSS:** `m1.css` · **Effort:** M
- **Registry:** `web/src/lessons/m1.js` (port from `docs/wireframe/src/lessons/m1.js`)
- **Uses shared shell:** yes

## Target wireframe screens
`docs/wireframe/screens/room-m1.html`, `room-m1-2-bind.html`, `room-m1-3-fade.html`, `room-m1-4-workbench.html`, `room-m1-5-numbers.html`, `room-m1-6-applied.html`, `room-m1-sw-showwork.html`, `room-m1-7-words.html`, `room-m1-practice.html`.

## Design gaps to close
- Stage 1 count box disabled until plates filled (opacity 0.5); enable on all-equal.
- Stage 2 addstrip "? =" → "4 + 4 + 4 =" on bind; hint becomes full strip.
- Stage 3 "plates checked" caption + collapse → "3 × 4 = ?".
- Stage 4 workbench BlockSandbox full UI (special layout, not 4-zone split).
- Stage 6 setup gates disabled after `setupOk`.
- Stage 7 optional scratch BlankSlate; QuestionBand hidden on stage 7 + practice.
- All 9 stage tabs in topbar with correct badges (1,2,3,4,5,6,sw,7,★).

## Dedup moves
- Move STAGES array (`AppM1.jsx:53-70`) → `lessons/m1.js` tabs; render tabs from registry via `LessonShell`.
- Pull `no/tag/title` from registry, not props.
- `STAGE_INTRO` (`:797-809`) → registry `intros` field (optional follow-on).
- Goal HTML (`:408-412`) → registry `goals`/`LessonGoal` (follow-on).
- Keep TutorRibbon/AnswerBar/HintRail usage (already shared); only data moves.

## Mechanics to PRESERVE (do not break)
- Drag pelmeni→plates, spill-back guard (SIZE/plate), right-click clear, numeric count box.
- Bind: tap plate → "+4" term; unlock total Slate when all bound.
- Fade: collapse 4+4+4 → 3×4; plates ghost.
- Workbench: BlockSandbox build to flag → `onSandboxSolve`.
- Applied two-row setup gate; Show Work ungraded ink gate; Words `gradeWordProduct`.
- Practice: `GenPracticeBoard` skill `MULT_EQUAL_GROUPS`.
- `useLessonScaffold` stage progression; SCAFFOLD_KEY mapping; reset → goStage(1).

## Playwright test plan (click REAL elements)
1. Stage 1: drag pelmeni to plate, count increments; add 5th → spill-back (warn, red outline); fill 3×4, count box unlocks; type 12, Check → advance to 2.
2. Stage 2: tap 3 plates → "4 + 4 + 4 = ?"; Slate unlocks; write 12, Check → advance.
3. Stage 3: click collapse → "3 × 4 = ?"; write 12 → advance.
4. Stage 4: drag 3 blocks of 4 to line → `onSandboxSolve`, Check enables → advance.
5. Stage 5: write 12 → advance.
6. Stage 6: write setup 3 and 4, Check setup → answer Slate unlocks; write 12 → advance.
7. Show Work: draw on BlankSlate → Next enables → advance.
8. Stage 7: QuestionBand hidden; write 10 (5×2) → end.
9. Practice: solve a generated problem; assert re-roll.
10. Tabs: click tab 3 then 1; assert stage state.
11. Responsive: <880px no overlap. 12. A11y: tablist/tab roles, plate aria-labels.

## Risks
Stage interdependencies (keep advance logic in `useLessonScaffold`); scattered stage state; Slate/BlankSlate canvas refs (never serialize to HTML); BlockSandbox non-split layout (add `layoutVariant`); SVG inlining (keep as components); voice keys travel with copy.

## Definition of done
Design matches wireframe · tabs/identity from registry · all 11+ Playwright checks pass with zero console/page errors · `npm run build` clean · vitest green · worktree + PR.

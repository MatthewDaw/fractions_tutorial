# Work Order — s1 (Subtraction · Same Denominators)

- **Component:** `web/src/AppSubtract.jsx` · **Route:** `#/s1` · **CSS:** `s1.css` · **Effort:** M
- **Registry:** `web/src/lessons/s1.js` (port from `docs/wireframe/src/lessons/s1.js`)
- **Uses shared shell:** yes · Disjoint; parallel-safe.

## Target wireframe screens
`docs/wireframe/src/screens/room-s1.js`, `room-s1-2-takeaway.js`, `room-s1-3-numbers.js`, `room-s1-4-words.js`, `room-s1-practice.js`.

## Design gaps to close
- STAGES inline; identity inline → registry.
- QuestionBand gate `stage !== 4 && !== 'practice'` (fragile — keep but document).
- Goal via LessonGoal; tabs via goStage — derive current from registry.
- `STAGE_INTRO(n)` (`:623-631`) → registry intros.

## Dedup moves
- STAGES, intro/goal copy → `lessons/s1.js`.
- Extract `LockedDenominatorSlate` wrapper (stages 2/3/4 same pattern).
- Generic `gradeSlate()` factory from checkTakeAway/checkNumbers/checkWords.
- Extract `<FractionPiece>`/`<UnitPiece>` ({count,den,broken,gone,draggable}).
- Lock+BigFrac hint → shared `<SubtractionRule>` hint.

## Mechanics to PRESERVE
- Stage 1 break-apart toggle + reassemble (doBreak awards on first break); Stage 2 pointer-drag pieces across trayTop (CH*0.58) — exactly 2 removed validation; Slate locked-den numerator-only stages; flashBad; Stage 4 optional BlankSlate + full-fraction Slate + readAloud; practice SUB_SAME_DEN; tab nav resets state; `brokenRef`/`takenRef` sync for drag handlers.

## Playwright test plan
1. Stage 1: solid row, click Break → is-broken, 5 pieces space, decomp-eq updates, cook cheer; reassemble toggles back; award on break.
2. Stage 2: drag piece below trayTop → is-gone, tray has-pieces; drag 2 → "2 taken"; locked den 8; submit ≠2 → flashBad; correct 2 + num 3 → award.
3. Stage 3: bigeq "7/8 − 3/8 = ?", numerator-only; write 4 → wrong; write correct → award.
4. Stage 4: WordProblem, read aloud, write 3/8 → award; wrong bottom → errorSignature.
5. Practice: solve, re-roll; wrong → flashBad.
6. Tabs reset; back; rewatch; reset → stage 1.
7. Shell integration (LessonBoard rail 396 / footer 140).

## Risks
brokenRef/takenRef sync (don't drop refs); drag math magic `CH*0.58`/`k=width/1280` drift with responsive width; Slate autoFocusKey; QuestionBand conditional fragile; shared slate state across write stages (numerator-only gating); initialStage resolution; GenPracticeBoard skill match; cook mood coverage.

## Definition of done
Wireframe match · registry-driven · Playwright passes, zero errors · build clean · vitest green · worktree + PR.

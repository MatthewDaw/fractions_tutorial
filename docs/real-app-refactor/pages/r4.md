# Work Order — r4 (Lesson 8 · Simplifying)

- **Component:** `web/src/AppR4.jsx` · **Route:** `#/r4` · **CSS:** `r4.css` · **Effort:** L
- **Registry:** `web/src/lessons/r4.js` (port from `docs/wireframe/src/lessons/r4.js`)
- **Uses shared shell:** yes · Disjoint files; safe to run in parallel.

## Target wireframe screens
`docs/wireframe/screens/room-r4-2-bind.html`, `-3-fade.html`, `-4-numbers.html`, `-5-applied.html`, `-6-words.html`, `-sw-showwork.html`, `-practice.html`.

## Design gaps to close
- Lesson number: AppR4 says "No.7" → wireframe "Lesson 8 · Simplifying"; pull `num:№8` from registry.
- Goal banner via LessonGoal (speech) vs wireframe div+data-vox — keep LessonGoal, move copy to registry.
- QuestionBand lead "the question".
- Rail dims `railW:452, footH:150` duplicated in JSX → registry `layout`.
- Words story tag (stage detection) "Babushka's Recipe" vs "Babushka's Kitchen".

## Dedup moves
- STAGES (`:65-74`, 8 stages) → registry tabs.
- `stageGoal()` (`:937-956`) → registry `goals`.
- `railWidth/footHeight` per-stage → registry `layout`.
- Extract `<GroupBar>` to shared component `{num,den,animKey,dim,lastK}`.
- Generic `submitAnswer(gradeFunc, requireLowest)` from the 5 submit handlers.

## Mechanics to PRESERVE
- Fuse/group ÷2/÷3 chips (divide num/den, snap anim, bounce on invalid); factor-pick (Fade); drag-ghost portal (>5px threshold, keyboard `e.detail===0`); Slate `onlyDigits`; `gradeSlate` cross-mult (8/12); applied setup gate (exact 8/12); showwork ink gate; `selfCorrectionsRef`++; `useLessonScaffold` + engine decisions (ReturnToKitchen); 1500ms auto-advance.

## Playwright test plan
1. Tabs 1→2→3→4→5→SW→6→★; is-done/is-active.
2. Group: ÷2 at 8/12 → 4/6 (snap, fill-edge fixed 440px); ÷3 at 8/12 → bounce; reach 2/3 → Next lights.
3. Bind: write 4/6 Check (3 stars if lowest, 2 if not); wrong amount → "not same amount"; ÷2 again → 2/3.
4. Fade: drag ÷2 onto eq → "8÷2/12÷2=4/6"; ÷5 decoy → bounce; write 4/6 → pass.
5. Numbers: 2/3 → 3 stars; 4/6 → 2 stars (can simplify); 3/5 → wrong.
6. Applied: 8/12 setup → unlock; 2/3 answer → advance; gate blocks early submit.
7. Show Work: ink → Next.
8. Words: no QuestionBand; 2/3 → done.
9. Practice: SIMPLIFY variants.
10. Drag: ghost appears >5px, is-hot over target, tap suppressed, keyboard fires. 11. Reset/back. 12. A11y. 13. Tutor states.

## Risks
Ruler tick geometry (ORIGIN=90/SPAN=660 exact); fill-edge 440px no drift; anim timing overlap vs 1500ms; gcd; `onlyDigits` paste edge; cross-mult sign; drag portal under CSS scale; speech queue; selfCorr count; stage-reset timing; setupOk not engine-graded; Words Band null; practice scaffold; requireLowest per stage.

## Definition of done
Wireframe match (№8) · registry tabs/goals/layout · Playwright passes, zero errors · build clean · vitest green · worktree + PR.

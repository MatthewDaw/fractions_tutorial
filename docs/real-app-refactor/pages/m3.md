# Work Order — m3 (Lesson 2 · Times Facts)

- **Component:** `web/src/AppM3.jsx` · **Route:** `#/m3` · **CSS:** `m3.css` · **Effort:** M
- **Registry:** `web/src/lessons/m3.js` (port from `docs/wireframe/src/lessons/m3.js`)
- **Uses shared shell:** yes

## Target wireframe screens
`docs/wireframe/src/lessons/m3.js` + `docs/wireframe/src/screens/room-m3*.js` (room-m3, -2-bind, -3-fade, -4-workbench, -5-numbers, -6-applied, -7-words, -sw-showwork).

## Design gaps to close
- STAGES array (`:61-77`) hardcoded → centralize in registry.
- `SURFACE_FORM` map (`:134-137`) — keep with component (skill config), not chrome.
- Lesson identity (№2, "Times Facts") inline → registry.
- Tab nav `goStage(toStageVal(key))` → derive current from registry tabs.
- QuestionBand per-stage gate stays component-side (pedagogical).
- `STAGE_INTRO` (`:795-807`) → registry `intros` (follow-on).

## Dedup moves
- STAGES, lesson №/tag/title, intro strings, per-stage captions, goal text → registry data fields.
- Keep TutorRibbon/AnswerBar/HintRail/QuestionBand component usage.

## Mechanics to PRESERVE
- SkipJar scoop tally (Stage 1); Slate `gradeProduct`; SkipLine drop validation w/ bounce (Stage 3); BlockSandbox build gate (Stage 4); fluency prompt cycling w/ mid-stage micro-reporting (Stage 5, final prompt advances); applied setup gate (Stage 6); BlankSlate ink gate (showwork). `solvedRef`/`selfCorrectionsRef` lifecycle.

## Playwright test plan
1. Stage 1: click scoop 7× → tally 8→56; write 56, Check → ok, Next.
2. Stage 3: drop wrong → bounce, not persisted; drop correct 24@idx2, 40@idx4; write 56, Check.
3. Stage 4: Slate disabled until built; drag 7×8 blocks to line → cap "groups are built"; write 56.
4. Stage 5: 3 prompt dots; solve 7×8, 8×1, 8×0 in sequence; final award.
5. Stage 6: write 7 and 8, Check setup → product Slate enabled; write 56.
6. Stage 7: scratch optional; write 56 → award.
7. Show Work: draw → Next enables → stage 7.
8. Tabs: click numbers tab → is-done on 1-5, is-active on 5; all 9 render.
9. Engine: `solvedRef` gates re-check; `say()` never crashes on missing audio.

## Risks
Fluency micro-reporting (only 3rd prompt advances); SkipLine sync validation (no deferred bounce); sandbox build gate timing; BlankSlate remount; cook ribbon personalization; `solvedRef`/`selfCorrectionsRef` ref semantics.

## Definition of done
Wireframe match · registry-driven tabs/identity · Playwright plan passes, zero errors · build clean · vitest green · worktree + PR.

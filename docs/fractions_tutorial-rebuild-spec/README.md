# fractions_tutorial — Rebuild Spec

What goes here: the entry point and reading order for this reverse-engineered
rebuild specification. Links to `constitution.md` (foundational, filled),
`_coverage/` (census, partition, ledger), and the per-section spec files below.
Slice workers append into the section files; a final synthesis step owns the
cross-cutting overview, glossary, ADRs, nav graph, and data-flow diagram.

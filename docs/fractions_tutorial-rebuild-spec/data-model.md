# Data Model

What goes here: the data shapes / schemas / important types — engine wire DTOs
(Event/Observation/MasteryEstimate/Decision/SkillNode), the skill-graph DAG, the
room/strand registry shapes, the settings record, localStorage persistence keys,
and the generated-problem envelope. The overall event→reduce→decision data-flow
diagram is SYNTHESIS-OWNED.

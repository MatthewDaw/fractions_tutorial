# Requirements

What goes here: the functional + non-functional requirements per subsystem —
what each slice's code must DO (learning goals per room, mastery/decision
behavior, generator guarantees, navigation, settings, audio, harness guarantees).
Slice workers append their owned requirements; numbering follows the source's
KTD/R/U references where present.

# Environment & Configuration

What goes here: env var NAMES + purpose (ElevenLabs key + per-character voice
ids — values NEVER recorded), engine PARAMS tunables + feature flags, Vite build
env (`import.meta.env.DEV`/`BASE_URL`), dev server host/ports, the settings
record + localStorage keys, and the `.ccss-contract.md` denominator constraint.

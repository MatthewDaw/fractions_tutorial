# Tasks

What goes here: the ordered rebuild task list — the implementation units needed to
reconstruct each subsystem, in dependency order (engine-core first as DAG root,
then generators/runtime, then lessons/shell/surfaces, then harness). Slice workers
append their tasks; synthesis sequences them.

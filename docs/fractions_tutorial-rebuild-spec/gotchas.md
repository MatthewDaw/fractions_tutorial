# Gotchas

What goes here: the non-obvious, load-bearing constraints and traps — the
prereq-prepend rule (credit.ts:97 docks `prereqs[length-1]`), engine purity (no
React/wall-clock in engine/), the advisory affect firewall (affect never reaches
gate.ts), boundary-only decision calls (R16), the `.js`→`.ts` Vite resolution,
onnxruntime-web optimizeDeps exclude, dev-only endpoints, and the CCSS denominator
contract. Slice workers append theirs.
